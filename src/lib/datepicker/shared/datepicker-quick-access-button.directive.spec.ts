import { DatepickerQuickAccessButtonDirective } from './datepicker-quick-access-button.directive';

describe('DatepickerQuickAccessButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerQuickAccessButtonDirective();
    expect(directive).toBeTruthy();
  });
});
