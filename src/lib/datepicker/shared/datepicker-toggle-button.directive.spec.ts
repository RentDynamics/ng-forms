import { DatepickerToggleButtonDirective } from './datepicker-toggle-button.directive';

describe('DatepickerToggleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerToggleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
